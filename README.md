# Placon Corporation — Production Website

Static GitHub Pages website configured for `placoncorp.com`.

## Important GitHub Pages files
- `CNAME` contains exactly: `placoncorp.com`
- `.nojekyll` is included.
- Publish from `main` → `/(root)`.

## Contact information
- Email: info@placoncorp.com
- Phone: 608-544-9769

## GitHub Pages / HTTPS
The HTML, CSS, and JavaScript do not determine whether GitHub's DNS check passes.
GitHub's DNS check depends on the public DNS records for `placoncorp.com`.

Because this site is published from a branch, keep the `CNAME` file in the repository root.
